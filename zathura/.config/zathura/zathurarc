# Include base16 colors configuration
include base16-zathura/base16-seti.config

set font			"inconsolata 9"

# Setting recolor-keephue true will keep any color your pdf has.
# If it is set to false, it will just be shades of grey. (bish)
set recolor-keephue		"true"

# Rest of your configs go here:
set selection-clipboard 	clipboard
